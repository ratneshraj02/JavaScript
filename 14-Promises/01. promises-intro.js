/* Promises */
/*
    Promises are special JS objects that are also concerded readability enhances they get immediately retuned from a function step to return a promise.

    They acts as placeholder or for the data use hope to get back from some feature task. We also attach the functionality we want to defer until the future task is done. and promise automatically handle execution of this functionality. so, promises do two things, one inside JS & one outside JS.

    It signs up the process required to run in the runtime & gives a placeholder in JS, which has value propertly.

    1. How we can create a promise?
    2. How can we consume a promise?
 */