/*
    Condtion inside if, else if always gets conveted to boolean.
    Any they can be used to proper case.
    > - greather than
    >= - greather then equal to
    < - Less than
    <= - less than equal to
    == - double equal
    === - triple equal
    != - not equal to 
    !== - not equal to 
*/
/*
    if(condition){
                    block : A banch of statement is called block.
    
    }

*/

//e.g :

if(true) {
    console.log("Hello");
}

if(false) {
    console.log("Hi")// This statement not executated because of false
}